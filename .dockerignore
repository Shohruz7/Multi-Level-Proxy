# Keep the build context small — images build from source, not artifacts.
target/
fuzz/
.git/
docs/
Documentation/
*.md
.DS_Store
flamegraph.svg
perf.data*
bench/results/
# The CDK app builds nothing the image needs, and node_modules is larger than
# the source tree it would be shipped beside.
infra/
