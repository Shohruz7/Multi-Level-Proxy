{
  "name": "h2proxy-infra",
  "version": "0.1.0",
  "private": true,
  "description": "AWS CDK stack for h2proxy: NLB + Graviton ASG + backends + load generator",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test \"dist/test/*.test.js\"",
    "synth": "tsc && cdk synth",
    "diff": "tsc && cdk diff"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "aws-cdk": "^2.174.0",
    "typescript": "~5.7.2"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.174.0",
    "constructs": "^10.4.2"
  }
}
