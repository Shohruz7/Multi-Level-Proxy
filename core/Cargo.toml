[package]
name = "h2proxy-core"
description = "Hand-built HTTP/2 protocol engine: framing, HPACK, streams, flow control, pooling, load balancing"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bytes.workspace = true
thiserror.workspace = true
tokio.workspace = true
tracing.workspace = true

[dev-dependencies]
h2.workspace = true
http.workspace = true
proptest.workspace = true
criterion.workspace = true

# The first use of the criterion dependency, which has been declared since week 1
# and unused since week 1. Week 7 adds per-frame work, so "it costs nothing"
# finally needs a number.
[[bench]]
name = "hot_path"
harness = false
