# Rust build artifacts
/target

# cargo-fuzz: build output and generated corpora/crashes (keep Cargo.toml,
# Cargo.lock, and fuzz_targets/)
/fuzz/target/
/fuzz/corpus/
/fuzz/artifacts/
/fuzz/coverage/

# Profiling outputs (cargo-flamegraph)
flamegraph.svg
perf.data*

# Benchmark harness: kept local. `baseline.sh` and the promoted reference
# number `baseline.csv` predate this rule and are already tracked, so they stay;
# everything else under bench/ — run artifacts and notes — is ignored.
/bench/

# macOS
.DS_Store

# Local-only planning and reference docs (kept out of git by request).
# The per-week plans stay local; Week-1/2-tasks.md predate the rule and are
# already tracked.
/docs/
/Documentation/Week3-8.md
/Documentation/*-plan.md
